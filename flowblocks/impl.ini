[block]
name = Implementation Notes
label = {{ this.impl }}

[fields.impl]
label = Implementation
type = select
choices = urxvt, xterm, vte, konsole, linuxvc, other
choice_labels = urxvt, xterm, vte, konsole, linuxvc, other
width = 1/4

[fields.level]
label = Support level
type = select
choices = full, partial, ignored, unparsable, disabled, todo
choice_labels = full, partial, ignored, unparsable(unintended visual effect), disabled (non default option), TODO
width = 1/4

[fields.since]
label = Since
type = string
width = 1/4

[fields.body]
label = Body
type = markdown+jinja2
