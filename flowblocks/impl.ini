[block]
name = Implementation Notes
label = {{ this.impl }}

[fields.impl]
label = Implementation
type = select
choices = xterm, vte, konsole
choice_labels = xterm, vte, konsole
width = 1/4

[fields.level]
label = Support level
type = select
choices = full, partial, ignored, unparsable
choice_labels = full, partial, ignored, unparsable(unintended visual effect)
width = 1/4

[fields.since]
label = Since
type = string
width = 1/4

[fields.body]
label = Body
type = markdown
